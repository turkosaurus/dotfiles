- `work new -r repo-name -b author/feature-name` should search for the repo in the repo dir, make sure main is current, and make a new branch and plan.toml and cd there
- after putting a task in toml, enumerate it with code examples
