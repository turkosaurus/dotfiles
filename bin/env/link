#!/usr/bin/env bash

# shellcheck source=/dev/null
source bin/util/set -eu

# path default
dot="$(pwd)/dotfiles"

# .bashrc
ln -f "$dot/.bashrc" "$HOME/.bashrc"

# .vscode
ln -f "$dot/.vscode/*" "$HOME/.vscode/"