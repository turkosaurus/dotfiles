#!/usr/bin/env bash

bin/util/require root

if ! htop --version &> /dev/null; then
    echo "Installing htop..."
    if ! sudo apt install htop -y; then
        echo "Failed to install htop."
        exit 1
    fi
    echo "htop installed!"
    exit 0
fi
